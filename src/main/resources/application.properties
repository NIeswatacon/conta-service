spring.application.name=CONTA
server.port=${PORT:8083}

# --- CONFIGURAÇÃO DO EUREKA ---
eureka.client.service-url.defaultZone=${EUREKA_URL}/eureka/apps/ # Added /eureka/apps/
eureka.instance.hostname=${RAILWAY_PRIVATE_IP:localhost} # Changed to RAILWAY_PRIVATE_IP
eureka.instance.ip-address=${RAILWAY_PRIVATE_IP:127.0.0.1} # Added explicit IP address
eureka.instance.prefer-ip-address=true
# Added explicit status and health check URLs if Railway requires them
eureka.instance.status-page-url=https://${RAILWAY_PUBLIC_DOMAIN}/info
eureka.instance.health-check-url=https://${RAILWAY_PUBLIC_DOMAIN}/health

# --- Existing Database and JPA/Hibernate Configuration ---
spring.datasource.url=jdbc:mysql://root:dFdirtwzLXuHbByhJxANFRywBdKYseRR@mainline.proxy.rlwy.net:36952/railway
spring.datasource.username=root
spring.datasource.password=dFdirtwzLXuHbByhJxANFRywBdKYseRR
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect
spring.jpa.properties.hibernate.format_sql=true

# --- Existing Security Configuration ---
jwt.secret=${JWT_SECRET}

# --- Existing Logging Levels ---
logging.level.org.hibernate.SQL=DEBUG
logging.level.org.hibernate.type.descriptor.sql.BasicBinder=TRACE
logging.level.com.zaxxer.hikari=DEBUG
logging.level.org.springframework.web=DEBUG
logging.level.unifor.pagamento=DEBUG
logging.level.com.netflix.discovery=DEBUG
logging.level.com.netflix.eureka=DEBUG